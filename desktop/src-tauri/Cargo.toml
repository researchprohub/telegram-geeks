[package]
name = "telegram-geeks-tauri"
version = "0.1.0"
description = "Tauri backend"
publish = false
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["custom-protocol"] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
