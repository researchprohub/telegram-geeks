[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "telegram-engagement-platform-anti-detection"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "loguru>=0.7.0",
    "redis[hiredis]>=5.0.0",
    "aiohttp>=3.9.0",
    "cryptography>=42.0.0",
]
